overwrite: true
schema: "http://localhost:3000/api/graphql"
documents: "**/*.graphql"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "urql-introspection"
